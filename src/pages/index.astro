---
import BlogCard from '@/components/BlogCard.astro'
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import { buttonVariants } from '@/components/ui/button'
import { SITE } from '@/consts'
import Layout from '@/layouts/Layout.astro'
import { getRecentPosts } from '@/lib/data-utils'
import { formatDate } from '@/lib/utils'

const blog = await getRecentPosts(SITE.featuredPostCount)
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="Home" />
  <section>
    <div class="rounded-lg border">
      <div class="flex flex-col space-y-1.5 p-6">
        <h3 class="text-gradient-my mb-6">
          <Link href="/about" class="border-b-4 border-violet-500 pb-2"
            >SeryiBaran</Link>
        </h3>
        <p class="text-muted-foreground text-sm">
          Личный сайт Ивана Музыка (aka SeryiBaran)
        </p>
      </div>
      <div class="p-6 pt-0">
        <p class="text-muted-foreground mb-2 text-sm">
          Привет! Я Иван Музы́ка, на момент последнего обновления сайта ({
            formatDate(new Date())
          }) мне 16 лет и я Frontend (изучающий Fullstack и DevOps) разработчик.
          Портфолио, контакты и прочая инфа обо мне находится на странице <Link
            href="/about"
            class="text-foreground"
            underline>Обо мне</Link
          >.
        </p>
      </div>
    </div>
  </section>
  <section class="flex flex-col gap-y-4">
    <h2 class="text-2xl font-medium">Посты</h2>
    <ul class="flex flex-col gap-y-4">
      {
        blog.map((post) => (
          <li>
            <BlogCard entry={post} />
          </li>
        ))
      }
    </ul>
    <div class="flex justify-center">
      <Link
        href="/blog"
        class={buttonVariants({ variant: 'ghost' }) + ' group'}
      >
        Посмотреть все посты <span
          class="ml-1.5 transition-transform group-hover:translate-x-1"
          >&rarr;</span
        >
      </Link>
    </div>
  </section>
</Layout>
